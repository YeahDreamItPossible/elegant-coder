export const ERROR = {
  SYSTEM: '系统错误,请稍后再试'
}

export const LOAD = Object.freeze({
  LOADING: '加载中...',
  HIDE_DELAY_TIME: 300, // loading 延时关闭时间
})

export const TOAST = Object.freeze({
  SUCCESS_TEXT: '操作成功',
  DURATION_TIME: 600
})

// 节流阀门
export const VALVE = Object.freeze({
  MAX_CAPACITY: 30
})

export const DELAY_TIME = Object.freeze({
})
